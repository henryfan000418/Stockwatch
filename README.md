# Stockwatch

Stockwatch is an agent workflow for monitoring financial news, discovering active industry themes, extracting stock tickers from the news flow, and generating research-ready company or industry reports.

## Project Root

Use this directory as the project root for all commands, docs, scripts, and future agent work:

```powershell
cd C:\Users\herry\Desktop\codex\stockwatch
```

Start with the workflow docs in `agent_workflow/README.md` and the design notes in `docs/agent_workflow_design.md`.